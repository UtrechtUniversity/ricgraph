This directory contains Python harvest scripts for Ricgraph.
Each script will harvest one source. New harvest scripts (for
new sources) should be added to this directory.

Directory 'harvest_multiple_sources' contains wrapper bash scripts that
call the python harvest scripts in this directory. Most often, these
are more convenient to harvest sources for Ricgraph.

